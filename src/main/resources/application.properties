 # ==============================
# = Spring Boot App Settings  =
# ==============================
spring.application.name=real-time-chat-app
server.port=8080

# ==============================
# = Database Configuration     =
# ==============================
spring.datasource.url=jdbc:mysql://localhost:3306/chat_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

# Optional: HikariCP connection pool settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# ==============================
# = JPA / Hibernate Settings   =
# ==============================
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ==============================
# = WebSocket Config (Optional if used) =
# ==============================
# spring.websocket.prefix=/ws
 